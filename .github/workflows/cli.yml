# Generated by ChatGPT
name: Terminal Proxy Workflow

on:
  workflow_dispatch:
    inputs:
      command:
        description: 'Select a command to execute'
        required: true
        default: 'php hyde list' # Default command

      arguments:
        description: 'Optional arguments to append to the command'
        required: false

jobs:
  execute_command:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      
      - name: Execute command
        run: |
          # Parse command input
          command="${{ github.event.inputs.command }}"
          arguments="${{ github.event.inputs.arguments }}"

          # Combine command with arguments
          full_command="${command} ${arguments}"

          # Execute the command
          echo "Executing command: $full_command"
          $full_command
